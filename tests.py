import unittest
from contact import *

class TestContactMethods(unittest.TestCase):

    def test_create_GUI(self):
        """
        we cannot test this method automatically. We need to do it manually. To test this, follow these steps.
        1) Run the app.
        2) Check if all the components are visible
        3) Check if all the buttons and functions working
        :return:
        """
        pass

    def test_add(self):
        """
        we cannot test this method automatically. We need to do it manually. To test this, follow these steps.
        1) Run the app.
        2) Enter data to the respective fields
        3) Click on Add button
        3) Check if that contact name is shown in the contact list
        :return:
        """
        pass

    def test_view(self):
        """
        we cannot test this method automatically. We need to do it manually. To test this, follow these steps.
        1) Run the app.
        2) Select any contact from the list
        3) Click on View button
        3) Check if the contact details are shown in the respective fields
        :return:
        """
        pass

    def test_delete(self):
        """
        we cannot test this method automatically. We need to do it manually. To test this, follow these steps.
        1) Run the app.
        2) Select the contact from list
        3) Click on Delete button
        3) Check if that contact name is not showing in the list anymore
        :return:
        """
        pass

    def test_create_db(self):
        """
        this method cannot be tested automatically. We need to do it manually. To test this, follow these steps.
        1) Run the app.
        2) Go to the directory where your app resides
        3) If file with name contacts.db is there, then test succeed
        :return:
        """
        pass